@model ComplianceAuditWeb.Models.OrganizationViewModel

@{
    ViewBag.Title = "AddGroupCompany";
}

<h2>AddGroupCompany</h2>

@using (Html.BeginForm()) 
{
    @Html.AntiForgeryToken()
    
    <div class="form-horizontal">
        <h4>OrganizationViewModel</h4>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        <div class="form-group">
            <div class="col-md-offset-2 col-md-10">

                @*@Html.LabelFor(m => m.CountryList);
                @Html.DropDownList("Country", Model.CountryList as SelectList);*@


                @*@Html.DropDownListFor(m => m.CountryList,(IEnumerable<SelectListItem>) ViewBag.CountryList, "-- Select Country--" );*@

                @Html.Partial("_Organization", Model);
            </div>
        </div>
    </div>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>
